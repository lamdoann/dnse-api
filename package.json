{
  "name": "dnse-api",
  "version": "0.1.0",
  "description": "Node.js & TypeScript SDK for the DNSE (Dòng Nắng Sài Gòn) OpenAPI: trading, accounts, orders & market data with HMAC HTTP-signature auth.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rimraf dist",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write \"src/**/*.ts\"",
    "test": "jest",
    "example": "node --env-file=.env --import tsx",
    "example:ohlc": "node --env-file=.env --import tsx examples/rest-ohlc-vn30-futures.ts",
    "example:account": "node --env-file=.env --import tsx examples/rest-account.ts",
    "example:market-data": "node --env-file=.env --import tsx examples/ws-market-data.ts",
    "example:trading": "node --env-file=.env --import tsx examples/ws-trading.ts",
    "example:derivatives": "node --env-file=.env --import tsx examples/derivatives-realtime.ts",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "dnse",
    "dnse-api",
    "entrade",
    "vietnam",
    "stock",
    "chung-khoan",
    "trading",
    "openapi",
    "hose",
    "hnx",
    "derivative",
    "market-data",
    "api",
    "sdk",
    "typescript"
  ],
  "author": "thanhlam.fit@gmail.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-org/dnse-api.git"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/ws": "^8.18.1",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.23.1",
    "typescript": "^5.4.0"
  }
}
